
<!--- Progress.stories.mdx --->

import { Meta, Story, Canvas } from '@storybook/addon-a';

import Progress from './progress';

<Meta title="Components/Progress" component={Progress} />

export const Template = (args) => <Progress {...args} />

## Progress

Button is the primary component. It has four possible states.

<Canvas>
  <Story name="primary" args={{
    percent: 30,
    theme: 'primary'
  }}>
    {Template.bind({})}
  </Story>

  <Story name="primary" args={{
    percent: 45,
    theme: 'danger'
  }}>
    {Template.bind({})}
  </Story>
</Canvas>
