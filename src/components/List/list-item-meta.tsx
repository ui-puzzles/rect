import React, { FC } from 'react';
import classNames from 'classnames';

import { ListItemMetaProps } from './interface';


const ListItemMeta: FC<ListItemMetaProps> = (props) => {
  const {
    className
  } = props;

  return (
    <div>
      
    </div>
  );
};

export default ListItemMeta;