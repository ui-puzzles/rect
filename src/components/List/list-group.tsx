import React, { FC } from 'react';
import classNames from 'classnames';

import { ListGroupProps } from './interface';


const ListGroup: FC<ListGroupProps<any>> = (props) => {
  const {
    className
  } = props;

  return (
    <div>
      
    </div>
  );
};

export default ListGroup;